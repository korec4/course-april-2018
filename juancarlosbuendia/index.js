'use strict';

const  URL_NAME = 'http://wwww.mi_sitio.com'
const object = {};



const  myfunction = (param = '') =>{
    //const boo = '';
    if (param === undefined){
        throw Error({
            type: 'FATAL',
            message: 'el vlor esta vacio'
        });
    }else{

    }

}

debugger;


const Array = [1,2,3,4];
const myFunction = (element) =>{
  document.getElementById('contenet').innerText;
};

Array.forEach(element => myFunction(element));


class MyClassNew  {

  functionA = () =>{};
}


for (let iterable of array){

    if(iterable.ui === 2){
        //break;
        continue;
        //return null;
    }else{

        throw Error('not a number 2 ');
    }
}


const promise = new Promise((resolve, reject) => {

      if (user.name === 'alfonso'){
          resolve(user);
      }else{
          reject('este no es el usuario correcto')
      }

});


promise
.then()
.catch();