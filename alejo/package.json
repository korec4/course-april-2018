{
  "name": "api-fake",
  "version": "1.0.0",
  "description": "api fake para curso front-end",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch ./db/database.json --port 3004",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alejandro Caravantes",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
